// eslint-disable-next-line @typescript-eslint/no-unused-vars
type ButtonProps = {
  color?: "default" | "primary" | "secondary";
  size?: "default" | "short" | "wide";
  icon?: string; // 아이콘 이름을 문자열로 받음
};

function Button() {
  return <div>Button</div>;
}

export default Button;
