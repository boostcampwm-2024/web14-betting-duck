# 모노레포에서 안정적인 패키지 관리를 위한 권장 설정
enable-pre-post-scripts=true    # 빌드 스크립트 필요 시
node-linker=isolated           # 패키지 독립성 보장
strict-peer-dependencies=false # 개발 편의성
auto-install-peers=true       # 의존성 자동 관리
prefer-workspace-packages=true # 로컬 패키지 우선
